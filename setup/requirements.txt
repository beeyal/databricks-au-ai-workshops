# ============================================================
# Databricks AU AI Workshops — Python Requirements
# ============================================================
# Install in your Databricks cluster init script or notebook:
#   %pip install -r /Workspace/path/to/requirements.txt
#
# For local development:
#   pip install -r requirements.txt
# ============================================================

# ------------------------------------------------------------
# Databricks SDKs and connectivity
# ------------------------------------------------------------
databricks-sdk==0.44.0          # Workspace, UC, Vector Search, Genie, Jobs APIs
databricks-connect==16.1.0      # Run Spark code locally against a remote cluster

# ------------------------------------------------------------
# LLM orchestration and LangChain integration
# ------------------------------------------------------------
databricks-langchain==0.4.3     # LangChain ChatDatabricks, DatabricksVectorSearch, embeddings
langchain==0.3.25               # Core LangChain framework
langchain-core==0.3.59          # Runnable, BaseMessage, tool abstractions
langchain-community==0.3.24     # Document loaders, retrievers, third-party integrations
langgraph==0.4.7                # Stateful multi-agent graph framework (used in Workshop 3)
langchain-mcp-adapters==0.1.3   # Wrap MCP servers as LangChain tools

# ------------------------------------------------------------
# OpenAI-compatible client (for Foundation Model API)
# ------------------------------------------------------------
openai==1.82.1                  # OpenAI-compatible client; used with Databricks FMAPI base_url

# ------------------------------------------------------------
# MLflow — experiment tracking and model governance
# ------------------------------------------------------------
mlflow==2.22.0                  # Tracking, tracing, model registry, evaluation

# ------------------------------------------------------------
# Data handling (workshop notebooks + local data generation)
# ------------------------------------------------------------
pandas==2.2.3                   # DataFrame operations for data prep and analysis
numpy==2.2.6                    # Numerical arrays, distributions, random generation
pyarrow==20.0.0                 # Arrow columnar format; required for Delta/Spark interop

# ------------------------------------------------------------
# Utilities
# ------------------------------------------------------------
python-dotenv==1.1.0            # Load .env files for local credential management
pydantic==2.11.5                # Data validation; used by LangChain tool definitions
tenacity==9.1.2                 # Retry logic for API calls (transient errors)
rich==14.0.0                    # Pretty terminal output for setup/preflight scripts
tqdm==4.67.1                    # Progress bars for long-running data ingestion steps
